info:
  name: 01 - Health Check (GET 200)
  type: http

http:
  method: GET
  url: "{{baseUrl}}/api/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
