info:
  name: 02 - Catálogo Completo (GET 200)
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/frutas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
