info:
  name: 04 - Detalle Existente (GET 200)
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/frutas/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
