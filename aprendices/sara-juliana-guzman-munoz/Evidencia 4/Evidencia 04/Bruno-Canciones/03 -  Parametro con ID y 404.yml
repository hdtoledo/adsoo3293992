info:
  name: 03 -  Parametro con ID y 404
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASEurl}}/api/canciones/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
