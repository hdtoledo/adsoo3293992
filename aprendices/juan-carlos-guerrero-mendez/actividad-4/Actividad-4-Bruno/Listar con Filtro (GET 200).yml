info:
  name: Listar con Filtro (GET 200)
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURL}}/api/productos?categoria=pantalla"
  params:
    - name: categoria
      value: pantalla
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
