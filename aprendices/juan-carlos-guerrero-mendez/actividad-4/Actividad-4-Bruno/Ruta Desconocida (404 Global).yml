info:
  name: Ruta Desconocida (404 Global)
  type: http

http:
  method: GET
  url: "{{BaseURL}}/api/fsdvgd"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
