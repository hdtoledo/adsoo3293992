info:
  name: Modificar Stock (PATCH 200)
  type: http

http:
  method: PATCH
  url: "{{BaseURL}}/api/productos/5/stock"
  body:
    type: json
    data: |-
      {
        "nuevoStock":16
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
