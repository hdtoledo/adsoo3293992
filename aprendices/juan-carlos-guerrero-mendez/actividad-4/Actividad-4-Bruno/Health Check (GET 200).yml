info:
  name: Health Check (GET 200)
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURL}}/api/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
