info:
  name: Detalle Inexistente (GET 404)
  type: http

http:
  method: GET
  url: "{{BaseURL}}/api/productos/999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
