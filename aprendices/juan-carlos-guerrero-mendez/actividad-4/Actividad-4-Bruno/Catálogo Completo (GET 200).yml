info:
  name: Catálogo Completo (GET 200)
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURL}}/api/productos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
