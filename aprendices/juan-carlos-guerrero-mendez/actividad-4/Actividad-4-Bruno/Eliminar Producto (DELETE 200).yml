info:
  name: Eliminar Producto (DELETE 200)
  type: http

http:
  method: DELETE
  url: "{{BaseURL}}/api/productos/13"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
