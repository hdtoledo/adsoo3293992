info:
  name: get-api-zoologico
  type: http

http:
  method: GET
  url: "{{baseUrl}}/api/zoologico"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
