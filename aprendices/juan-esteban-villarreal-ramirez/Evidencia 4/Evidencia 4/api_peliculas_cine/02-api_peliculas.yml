info:
  name: 02-api_peliculas
  type: http
  seq: 2

http:
  method: GET
  url: "{{localUrl}}/api/peliculas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
