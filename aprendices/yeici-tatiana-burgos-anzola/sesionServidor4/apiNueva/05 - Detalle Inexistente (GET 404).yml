info:
  name: 05 - Detalle Inexistente (GET 404)
  type: http

http:
  method: GET
  url: "{{baseUrl}}/api/productos/999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
