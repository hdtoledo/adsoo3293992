info:
  name: 09 - Eliminar Producto (DELETE 200)
  type: http

http:
  method: DELETE
  url: "{{baseUrl}}/api/productos/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
