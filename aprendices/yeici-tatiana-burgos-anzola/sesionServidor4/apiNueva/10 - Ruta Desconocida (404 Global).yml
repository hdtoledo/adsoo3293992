info:
  name: 10 - Ruta Desconocida (404 Global)
  type: http

http:
  method: GET
  url: "{{baseUrl}}/api/ruta-fantasma-adso"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
