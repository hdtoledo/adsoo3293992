info:
  name: 02 - Catálogo Completo (GET 200)
  type: http

http:
  method: GET
  url: "{{baseUrl}}/api/productos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
